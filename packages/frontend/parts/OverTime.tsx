import {StartOverTime,EndOverTime} from '../parts/Modal';

export const OverTime = () => {

    return (
        <div className="mt-2">
            <StartOverTime />
            <EndOverTime />
        </div>
    )

}